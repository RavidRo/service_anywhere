{
   "type": "sqlite",
   "host": "localhost",
   "port": 3306,
   "username": "myUserName",
   "password": "password",
   "database": "DanHotelsDB",
   "synchronize": true,
   "logging": true,
   "entities":["./build/server/entities/**/*.js"],
   "migrations": [
      "/migration/**/*.{ts, js}"
    ],
    "suscribers": [
      "/suscriber/**/*.{ts, js}"
    ],
   "cli": {
      "entitiesDir": "/entity",
      "migrationsDir": "/migration",
      "subscribersDir": "/subscriber"
   }
}